$('#endless_list').append("<%= escape_javascript(render partial: 'thumbnails', collection: @venues, as: 'venue' || "There are no venues at the moment") %>");

<% if @venues.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate(@venues) %>');
<% else %>
	$('.pagination').remove();
<% end %>
<%# using styling from carousel-theme.css %>

        <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
			<div class="container">
				<div class="navbar-header">				

					<% if current_user && current_user.account_type == "admin" %>								
						<button type="button" class="navbar-toggle slide-left" data-toggle="collapse" data-target=".navbar-collapse">
							<span class="sr-only">Toggle navigation</span>
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
							<span class="icon-bar"></span>
						</button>	
					<% else %>
						<%= link_to "Log in", '#!', class:'navbar-toggle btn btn-primary btn-xs slide-left', id:'log_in_button', :data => { :button => 'login', :toggle => 'modal', :target => '#loginModal' }%>
<% if false %>													
						<button type="button" class="btn btn-primary btn-xs" data-toggle="collapse" data-target=".navbar-collapse">
							Sign In
						</button>
<% end %>
<% if false %>
						<div class="navbar-toggle">
							<%= link_to "Log in", '#', class:'btn btn-primary btn-xs', id:'login-link', :"data-toggle" => "modal", :"data-target" => "#loginModal" %>
						</div>
<% end %>
					<% end %>
					<a class="navbar-brand" href="#">Project name</a>
				</div>
				<div class="collapse navbar-collapse">
					<% if current_user && current_user.account_type == "admin" %>									
						<%= render 'layouts/navigation_links' %>
					<% end %>
					<%= render 'layouts/navigation_links_right' %>
				</div>
				
			</div>
        </div>
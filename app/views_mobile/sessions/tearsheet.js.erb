//alert("Used AJAX to get tearsheet");
$('#dynamic-content').html("<%= escape_javascript(render partial: 'tearsheet', locals: {venue: @venue, venue_events: @venue_events}) %>");
$('#filter-container').remove();
$('.footer-fixed-bottom').hide();
truncateText();
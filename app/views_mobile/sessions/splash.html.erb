	<!-- Carousel -->
	<div id="myCarousel" class="carousel slide">
	<% if false %>
		SQL: <%= @venues.to_sql %>
	<% end %>
		<%= render 'sessions/carousel' %>
	</div><!-- /.carousel -->

	<!-- Jumbotron -->
	<div class="jumbotron dark">
		<h3><strong>CHECK OUT THE VIBE</strong></h3>
		<p class="grey-text">Why read reviews when you can see for yourself?  We provide videos. You choose the venue.</p>
	</div>
	
	<!-- Jumbotron -->
	<div class="jumbotron">
		<P>Watch videos</p>
		<p>Discover events</p>
		<p>Find specials</p>
<% if false %>		
		<a class="btn btn-primary btn-sm" href="#newUser">Create Your Free Account</a>
<% end %>
		<%= button_tag class:"btn btn-primary slide-left sign_up_button", :data => { :button => 'signup' } do %>
			Create Your Free Account
		<% end %>
	</div>